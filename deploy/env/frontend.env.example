# =============================================================================
# Frontend production env — copy thành /var/www/sai-gon-work/frontend/.env.production
# (Next.js sẽ tự pick file này khi build với NODE_ENV=production)
# =============================================================================

# API base — qua nginx, KHÔNG http://localhost:4000 trong production
NEXT_PUBLIC_API_URL=https://sai-gon-work.lemiex.us/api
